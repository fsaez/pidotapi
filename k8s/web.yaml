apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
spec:
  template:
    spec:
      $setElementOrder/containers:
      - name: web
      containers:
      - image: TO_BE_MODIFIED
        name: web
